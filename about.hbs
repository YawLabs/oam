# Third-party licenses

`oam` is distributed as a statically-linked binary that embeds the Rust crates
listed below, plus the V8 JavaScript engine. This file reproduces their license
notices, as required for binary redistribution.

oam's own source is licensed Apache-2.0 (see `LICENSE`); nothing here changes
that. Generated by `cargo about generate about.hbs -o THIRD_PARTY_LICENSES.md` --
do not edit by hand, regenerate when the dependency graph changes.

## Summary

{{#each overview}}
- {{{name}}} -- {{count}} crate(s)
{{/each}}

---

{{#each licenses}}
## {{{name}}}

Used by:

{{#each used_by}}
- {{{crate.name}}} {{{crate.version}}}
{{/each}}

```text
{{{text}}}
```

{{/each}}
